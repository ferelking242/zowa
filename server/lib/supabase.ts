// Supabase replaced by local PostgreSQL — this file is kept as a stub
// so existing imports don't break during the transition.
export const supabase = null as any;
export const supabaseConfig = { url: '', anonKey: '' };
